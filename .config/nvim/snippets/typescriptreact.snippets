snippet state
	const [${1:name}, set${1/.*/\=v:lua.pascal(submatch(0))/}] = useState(${2:defaultValue})

snippet effect
	useState(()=>{
		${1}
	}, [])

snippet asyncEffect
	useState(() => {
		(async () => {
			${1}
		})()
	}, [])

snippet icon
	import type { SVGProps } from 'react'
	import SVG from './index.svg'

	export const ${1/.*/\=v:lua.pascal(submatch(0))/}Icon = (props: SVGProps<SVGElement>) => (
		<SVG viewBox={'0 0 24 24'} {...props} />
	)

